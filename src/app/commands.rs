// Command processing module
// Will be implemented as features are added

#[allow(dead_code)] // Will be used when command processing is implemented
pub enum Command {
    Quit,
    Refresh,
    // More commands to be added
}
